[package]
name = "gaia-core"
version = "0.1.0"
edition = "2021"
description = "DreamForge core: ECS, protocol, scheduler, and package registry"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
