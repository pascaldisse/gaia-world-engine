[package]
name = "gaia-ecs"
version = "0.1.0"
edition = "2021"
description = "Runtime-schema archetype ECS matching GAIA's reference scheduler"
license = "MIT"


[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
