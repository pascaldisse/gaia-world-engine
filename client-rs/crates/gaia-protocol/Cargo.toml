[package]
name = "gaia-protocol"
version = "0.1.0"
edition = "2021"
description = "Serde protocol types for GAIA world documents and wire messages"
license = "MIT"


[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
